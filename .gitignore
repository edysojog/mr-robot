node_modules/
release/
*.log
.env

# Local launch scripts -- these hold API keys inline.
.run_*.ps1

# Loose screenshots dropped in the project root while working; anything the
# README actually uses lives in docs/ and is committed.
/*.png
/*.gif
